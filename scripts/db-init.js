// One-time local D1 bootstrap. Runs `wrangler d1 create DB` if the
// database doesn't exist, applies migrations locally, then seeds the
// first HK Leader account. Filled in as task 13 lands.

console.log("db-init: to be implemented in task 13");
console.log("for now: wrangler d1 create njy  &&  npm run db:migrate:local");
